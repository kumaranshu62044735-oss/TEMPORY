<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>👑 First Website 👑</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #000000, #4b006e, #ffcc00);
      color: gold;
      font-family: 'Poppins', sans-serif;
      text-align: center;
      overflow: hidden;
    }
    h1 {
      margin-top: 100px;
      font-size: 50px;
      text-shadow: 0 0 20px gold, 0 0 40px purple;
      animation: glow 2s infinite alternate;
    }
    @keyframes glow {
      from { text-shadow: 0 0 20px gold; }
      to { text-shadow: 0 0 40px purple; }
    }
    p {
      font-size: 17px;
      color: #fff;
      text-shadow: 0 0 5px #ffcc00;
    }
    button {
      background: gold;
      border: none;
      padding: 15px 30px;
      font-size: 18px;
      border-radius: 30px;
      margin-top: 40px;
      cursor: pointer;
      transition: 0.3s;
    }
    button:hover {
      background: white;
      color: black;
      transform: scale(1.1);
    }
    .sparkle {
      position: absolute;
      width: 6px;
      height: 6px;
      background: gold;
      border-radius: 50%;
      animation: sparkle 2s infinite;
    }
    @keyframes sparkle {
      0% { transform: translateY(0) scale(1); opacity: 1; }
      100% { transform: translateY(-100vh) scale(0); opacity: 0; }
    }
  </style>
</head>
<body>
  <h1>🔥 I am Anshu !! ✨ </h1>
  <p>Welcome to my first ever 24/7 live site!</p>
  <button onclick="alert('Thanks for visiting 👑')">Click!</button>

  <script>
    // Random sparkle effect
    setInterval(() => {
      const s = document.createElement('div');
      s.className = 'sparkle';
      s.style.left = Math.random() * 100 + 'vw';
      s.style.top = '100vh';
      document.body.appendChild(s);
      setTimeout(() => s.remove(), 2000);
    }, 150);
  </script>
</body>
</html>
